//TODO: this needs to run in sandbox environment

/*import { Identity } from "@semaphore-protocol/identity";
import { generateProof, FullProof } from "@semaphore-protocol/proof";
import { Group } from "@semaphore-protocol/group";

export const generateProofInSandbox = async (_identity: Identity, _group: Group, _groupId: string, _signal: number ): Promise<FullProof> =>  {
    return await generateProof(_identity,_group,_groupId,_signal);
};*/
